version: "3.8"

services:
  db:
    image: postgres:14-alpine
    restart: always
    environment:
      POSTGRES_USER: ${PG__USER}
      POSTGRES_PASSWORD: ${PG__PASSWORD}
      POSTGRES_DB: ${PG__DBNAME}
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
      - ./sql/schema.sql:/docker-entrypoint-initdb.d/01_schema.sql
      - ./sql/travel_schema.sql:/docker-entrypoint-initdb.d/02_travel_schema.sql

  app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      PG__USER: ${PG__USER}
      PG__PASSWORD: ${PG__PASSWORD}
      PG__DBNAME: ${PG__DBNAME}
      PG__HOST: db
      PG__URL: postgresql://${PG__USER}:${PG__PASSWORD}@db:5432/${PG__DBNAME}
      PG__POOL__MAX_SIZE: ${PG__POOL__MAX_SIZE}

      ADMIN_USER: ${ADMIN_USER}
      ADMIN_PASS: ${ADMIN_PASS}
      JWT_SECRET: ${JWT_SECRET}
      COOKIE_SECURE: ${COOKIE_SECURE:-false}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-http://localhost:5777,https://www.why2log.com,https://why2log.com}
    depends_on:
      - db
    ports:
      - "8080:8080"

volumes:
  db_data:
